enum CasualStatus { initial, loading, success, failure, notAuthorized }

enum ImageType { asset, network, file, memory }

enum Priority { low, medium, high }

enum ProjectVisibility { public, private }

enum ProjectType { mobile, website, systemAnalysis, softwareTestisng, maintain }

enum ProjectStatus { pended, inProgress, inReview, completed, canceled }

enum CooperationType { analysis, development, test, managment }

enum TaskStatus { toDo, inProgress, inReview, completed, canceled }

enum FieldStatus { approved, unApproved, seen, nonSeen, active, unActive }
