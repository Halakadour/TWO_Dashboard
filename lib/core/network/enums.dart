// ignore: constant_identifier_names
enum CasualStatus { initial, loading, success, failure, not_authorized }

enum ImageType { asset, network, file, memory }

enum TaskPriority { low, medium, high }

enum ProjectVisibility { public, private }

enum ProjectType { mobile, website, systemAnalysis, softwareTesting, maintain }

enum TaskStatus { pended, toDo, inProgress, inReview, completed, canceled }

enum SprintStatus { pended, started, completed }

enum CooperationType { analysis, development, test, managment }

enum FieldStatus {
  approved,
  unApproved,
  seen,
  nonSeen,
  active,
  unActive,
  needed,
  nonNeeded,
}
